# Compliance Remediation Matrix

**Host:** {{ inventory_hostname }}
**Policy:** {{ target_policy }}
**Generated:** {{ ansible_date_time.iso8601 }}
**Reboot required:** {{ 'YES' if reboot_needed else 'NO' }}

## Summary

| Severity | Count |
|---|---|
| High | {{ high_rules | length }} |
| Medium | {{ medium_rules | length }} |
| Low | {{ low_rules | length }} |
| **Total failed** | {{ failed_rules_count }} |
| Passed | {{ passed_rules_count }} |

## High-Severity Findings (remediate first)

{% for rule in high_rules %}
### {{ rule.title }}
- **Rule ID:** `{{ rule.id }}`
- **Automated fix available:** {{ 'Yes' if rule.has_snippet else 'No — manual remediation required' }}
- **Requires reboot:** {{ 'Yes' if rule.needs_reboot else 'No' }}

{% else %}
_No high-severity findings._
{% endfor %}

## Medium-Severity Findings

{% for rule in medium_rules %}
- `{{ rule.id }}` — {{ rule.title }} {{ '(reboot required)' if rule.needs_reboot else '' }}
{% else %}
_No medium-severity findings._
{% endfor %}

## Low-Severity Findings

{% for rule in low_rules %}
- `{{ rule.id }}` — {{ rule.title }}
{% else %}
_No low-severity findings._
{% endfor %}

---
*Generated by the FSI compliance automation pipeline. This report identifies findings; it does not apply fixes. See the companion remediation job template for automated fixes on rules with an available snippet.*
